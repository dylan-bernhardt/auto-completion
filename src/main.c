#include<stdio.h>
#include<string.h>
#include<stdlib.h>
#include"tree.h"
#include"auto_completion.h"
#include"menu.h"
#include"dico.h"

int main(void)
{
    // Root root;
    // init_tree(&root);
    // branch_words_from_a_file(root, "../txt/mots_courants.txt");
    // print_words_from_string(&root, "ou", 2);
    // start();
    // int choice = choices(50);
    // switch(choice)
    // {
    //     case(5) :
        
    //         system("firefox https://www.youtube.com/watch?v=_Yhyp-_hX2s");
            
    // }

    // List *ht[HASH_SIZE];
    // init_table(ht);
    // create_hash_table_from_file(ht, "../txt/dictionnaire.txt",3);
    // free_table(ht);
    // create_hash_table_from_file(ht, "../txt/dictionnaire.txt", 5);
    // print_table(ht);
    my_app();
    return 0;
    
    
}